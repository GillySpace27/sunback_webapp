.DS_Store
.vscode/settings.json
dep/*
*.png
# Real favicon assets (rasterized from api/favicon.svg, 2026-08-15) — the
# blanket *.png above is for render output/scratch, not brand assets.
!api/favicon-16.png
!api/favicon-32.png
!api/apple-touch-icon.png
!web3d/public/apple-touch-icon.png
*.fits
*.npz
*.pyc
api/__pycache__/main.cpython-312.pyc
.sunpy/
# Durable data files — live on the Render persistent disk in prod,
# created locally in dev. Never commit (feedback may contain PII).
feedback.jsonl
approved_catalog.json
product_stats.json
product_stats.json.tmp
default_cache/
api/certs/auto_appended.pem

# Migration working data (Render disk mirror + built worker assets)
infra/data_mirror/
infra/worker/public/
infra/worker/node_modules/
infra/worker/.wrangler/

# Committed secret removed from tracking (rotate PRINTFUL_API_KEY + purge history separately)
.env

# Agent session data. .claude/ holds per-session transcripts and worktrees and
# reached 1.2 GB in the main checkout on 2026-08-15 while still untracked but
# NOT ignored, which put it one `git add -A` away from being committed.
.claude/
.serena/
